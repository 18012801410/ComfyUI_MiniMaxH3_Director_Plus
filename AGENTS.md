# 通用项目工程规范

本文件定义现代软件项目的默认工程规则。子目录可以通过更近的 `AGENTS.md` 补充或细化规则，但不得无说明地削弱安全、验证和数据完整性要求。

## 规则优先级

规则按以下顺序生效：用户当前明确要求、距离目标文件最近的 `AGENTS.md`、项目根目录 `AGENTS.md`、通用默认约定。发生冲突时遵循优先级更高的规则，并在交付说明中指出影响结果的重要冲突。

## 变更纪律

- 修改前先理解所属模块、现有调用方、公开接口、配置方式和失败模式。
- 保持变更聚焦。能够独立审查的重构、行为修改和生成物更新应当拆分。
- 非平凡变更必须在同一变更中新增或更新 `.agents/notes/` 下的决策记录；纯机械性或局部编辑可以豁免。
- 先修改权威源，再通过生成器更新派生产物；不得直接维护本应生成的文件。
- 除非存在当前使用方或明确的兼容性承诺，不要为已废弃行为增加兼容分支。
- 不修改与任务无关的用户变更；工作区不干净时，应区分本次改动与既有改动。

## 设计与实现

- 显式表达所有权、生命周期、配置和错误行为，避免隐藏默认值和隐式全局状态。
- 在解析器、配置、持久化、队列、进程、工作线程、模型或工具输入以及网络协议边界进行运行时校验。对同进程内受静态类型约束的调用，不重复增加无依据的防御性校验。
- 注册、订阅和资源申请必须提供清理方式；销毁完成后不得遗留仍可产生副作用的任务、监听器或句柄。
- 保持公开 API 最小化。只服务于单个调用方的行为应放在调用方或私有能力中，而不是扩大通用接口。
- 运行时不变量应验证真实状态关系或权威事件，不应只检查方法、元数据或注册项是否存在。
- 能由类型系统表达的不变量优先交给类型系统；外部输入和持久数据必须运行时验证。
- 配置错误应在能够确定问题的最早位置明确失败，不得静默跳过或偷偷回退。
- 引入依赖前比较维护状态、依赖体积、剩余适配代码和净删除量；只有确实减少项目自有复杂度时才采用。

## 测试与验证证据

- 根据受影响面选择验证：局部行为用聚焦单元测试，组装路径用集成或端到端测试，用户或模型可见输出用快照，发布产物用构建和冒烟测试。
- 每项行为变更都必须有能观察该行为的测试，或明确说明为何无法通过测试验证。
- 测试必须能因目标回归而失败，并观察行为、状态、事件、输出或资源释放，而不是复述实现细节。
- 本地运行覆盖风险面的最小可信检查；除非变更确实跨越整个仓库、正在诊断 CI，或用户明确要求，否则不机械地运行完整测试集。
- 不因即将提交或推送而重复已经通过且未被后续改动影响的检查。
- 只报告实际运行过的命令与结果；环境限制、失败和外部待完成检查必须如实标明。

## 文档与技术文字

- 文档说明代码无法直接表达的约束，包括输入输出、所有权、时序、取消、持久性、副作用、失败方式和安全限制。
- 注释解释非显然的约束或原因，不复述控制流程、测试步骤、审查历史或代码表面事实。
- 每项长期事实只设一个权威说明位置；其他位置保留必要的局部约束并链接到权威来源。
- 提示词、诊断信息、CLI 文本和 UI 字符串属于产品行为；修改后应通过相应测试或快照验证。
- 正式文档描述当前事实，不泄露内部推理过程，不保留审查叙事。

## 安全与仓库卫生

- 不提交凭据、令牌、私钥、生成的秘密信息或含敏感值的本地环境文件。
- 执行删除、覆盖、迁移、历史重写或外部发布前，确认准确目标、影响范围和恢复方式。
- 必须重写远端历史时使用带已观察远端提交值的 `--force-with-lease`，禁止使用裸 `--force`。
- 文本文件以且仅以一个换行符结尾；发布前检查空白错误、最终差异和仓库状态。
- 不通过降低测试阈值、跳过检查或扩大异常规则来掩盖真实失败。

## 完成标准

声称任务完成前，应确认请求的行为已经实现，相关测试和文档已经更新，适当检查已经运行，最终差异没有意外文件，并说明已知限制、未运行检查和剩余风险。

## 项目级扩展

项目可以在更近的规则文件中增加语言、框架、部署、可访问性、本地化、发布和合规要求。硬性规则应说明其保护的具体风险，并在可行时提供自动化检查。

## 项目 Agent 工作协议

本仓库使用 `ai-software-factory` 技能组织开发流程，产物有固定落盘位置。任何涉及本项目（PreViz Studio）的规划、实现、调试、重构或发布任务，必须遵守以下协议。

### 启动工作前

1. 加载 `ai-software-factory` 技能并遵循其工作流（新功能 / 缺陷修复 / 重构 / 发布）；涉及技术选型、依赖引入或框架 API 变更时，同时加载 `technology-reconnaissance` 技能。
2. 读取项目状态与规格：
   - `.agents/project/specifications/PROJECT_STATE.md` —— 当前阶段、当前任务、已完成、决策、阻塞项；
   - `.agents/project/specifications/DEVELOPMENT_PLAN.md` —— 阶段定义与任务序列；
   - 与本次任务相关的规格文档（`ARCHITECTURE.md`、`DOMAIN_MODEL.md`、`API_SPEC.md`、`TECH_SPEC.md`、`UI_SPEC.md`、`AI_SPEC.md`、`AI_RULES.md` 等，均位于 `.agents/project/specifications/`）。
3. 从 `.agents/project/tasks/` 定位当前 `TASK-<id>.md`，以其"范围 / 非目标 / 验收标准"为本次工作边界；不存在任务文件时，先按规格生成工作流创建任务，不得直接大范围编码。

### 产物落盘位置（强制）

| 产物 | 位置 |
|---|---|
| 规格文档（PROJECT_BRIEF…PROJECT_STATE、IMPLEMENTATION_PLAN、SPECIFICATION_CONSISTENCY_CHECK） | `.agents/project/specifications/` |
| 实现任务（TASK-<id>.md） | `.agents/project/tasks/` |
| 技术侦察记录 | `.agents/project/reconnaissance/YYYY-MM-DD-<topic>.md` |
| 长期决策记录 | `.agents/notes/`（格式见 `.agents/notes/README.md`） |
| 已批准的设计文档 | `docs/superpowers/specs/` |

禁止把上述产物写入仓库根目录、`docs/` 或根级 `tasks/`；规格、任务、侦察记录与决策记录不得混放。

### 完成工作前

按 `ai-software-factory` 的检查清单自查（规格一致性 / 编码 / 发布），并更新 `.agents/project/specifications/PROJECT_STATE.md`（当前任务状态、已完成、最近变更、交接说明）；技术侦察若产出了需要复查的结论，落盘到 `.agents/project/reconnaissance/`。

## 语言偏好

- 所有对话、注释、提交信息均使用中文
